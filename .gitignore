node_modules
dist
dist-ssr
.env
.env.local
.env.*.local
*.log
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea

# Legacy .NET reference code — kept local only
_reference_dotnet/

# Firebase CLI cache
.firebase/

# Proprietary catalog data — live xlsx source for parts/services/consumables,
# kept local only. Ingested into Firestore at Round 13.
MG Cavite Database/

# One-off inspection / migration scripts — not shipped with the app.
scripts/
.vercel
